<Page x:Class="WpfApp_DataBinding_EF.Pages.InterestGroupsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp_DataBinding_EF.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="InterestGroupsPage">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Верхняя панель с кнопками -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" >
            <Button Content="Добавить"
                    Click="AddGroup_Click"/>
            <Button Content="Изменить"

                    Click="EditGroup_Click"/>
            <Button Content="Удалить"

                    Click="DeleteGroup_Click"/>
            <Button Content="Назад"

                    Click="Back_Click"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,3">
            <TextBlock Text="ID" Width="40"/>
            <TextBlock Text="Название" Width="150" Margin="10,0,0,0"/>
            <TextBlock Text="Описание" Width="250" Margin="10,0,0,0"/>
            <TextBlock Text="Участников" Width="60" Margin="10,0,0,0"/>
        </StackPanel>
        <!-- Список групп (ListView) -->
        <ListView x:Name="GroupsList"
          Grid.Row="2"
          ItemsSource="{Binding GroupsService.InterestGroups}"
          SelectedItem="{Binding SelectedGroup, Mode=TwoWay}"
          MouseDoubleClick="GroupsList_MouseDoubleClick">

            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Id}" Width="40"/>

                        <TextBlock Text="{Binding Title}"
                           Margin="10,0,0,0"
                           Width="150"/>

                        <TextBlock Text="{Binding Description}"
                           Margin="10,0,0,0"
                           Width="250"/>

                        <TextBlock Text="{Binding UserGroups.Count}"
                           Margin="10,0,0,0"
                           Width="60"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>

        </ListView>
    </Grid>
</Page>
